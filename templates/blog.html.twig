{% extends 'base.html.twig' %}

{% block body %}
<h1>Bienvenido a mi blog</h1>
<div class="container">
  <div class="article-list">
    {% for article in articles %}
    {{ include('article_teaser.html.twig') }}
    {% endfor %}
  </div>
</div>
{% endblock body %}